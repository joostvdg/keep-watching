-- CREATE TABLE IF NOT EXISTS Watcher
-- (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     identifier VARCHAR(255) UNIQUE
--     );
-- insert initial Watcher
INSERT INTO Watcher (id, name, identifier) VALUES (1, 'test', 'test');
